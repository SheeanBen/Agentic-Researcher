# Copy this file to `.env` or `.env.local` and fill in a valid key.
OPENAI_API_KEY=your_openai_api_key_here
# Optional if you use a compatible gateway:
# OPENAI_BASE_URL=https://api.openai.com/v1/responses
